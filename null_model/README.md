# null_model

Baseline model that predicts the training set mean for all 10 measurements. No neural network -- just computes the average of each measurement across the training set and uses that as the prediction for every test sample.

## Usage
```bash
cd null_model
python3 null_model.py
```
